name: work
root: <%= ENV.fetch('REPOS', File.expand_path('~/git')) %>

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
        - git status
  - shell:
      panes:
        -
