#!/usr/bin/env bash

# Compatibility loader for configurations that still source ~/scripts/fzf-git.
# The shared file defines all fzf and Git picker functions.
# shellcheck source=/dev/null
[[ -r "$HOME/.shellrc-common" ]] && source "$HOME/.shellrc-common"
